# Adding news posts

Create `src/content/updates/your-slug.md` with frontmatter (`date`, optional `title`, `summary` for list previews, `tag`, `image`, `externalLink`) and markdown body for the full post at `/news/your-slug/`. Homepage and `/news/` show short teasers only (latest 6 on home).
