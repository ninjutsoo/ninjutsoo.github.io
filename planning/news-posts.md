# Adding news posts

Create `src/content/updates/your-slug.md` with frontmatter (`date`, optional `title`, `tag`, `image`, `externalLink`) and markdown body. Posts appear on the homepage feed (latest 6) and on `/news/`.
